name: RADHA Private Verification Relay

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout exact private development head
        uses: actions/checkout@v4
        with:
          repository: ${{ secrets.RADHA_PRIVATE_REPOSITORY || secrets.LAS_PRIVATE_REPOSITORY }}
          token: ${{ secrets.RADHA_PRIVATE_READ_TOKEN || secrets.LAS_PRIVATE_READ_TOKEN }}
          ref: work/birth-substrate
          path: private
          persist-credentials: false
          fetch-depth: 1

      - name: Record privacy-safe revision proof
        shell: bash
        run: |
          set -euo pipefail
          cd private
          sha="$(git rev-parse HEAD)"
          proof="$(printf '%s' "$sha" | sha256sum | awk '{print $1}')"
          echo "RADHA_PRIVATE_SHA256=$proof"

      - name: Install pinned Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.98.1 --profile minimal --component rustfmt --component clippy
          rustup override set 1.98.1 --path private
          rustc +1.98.1 --version
          cargo +1.98.1 --version

      - name: Format gate
        id: fmt
        continue-on-error: true
        shell: bash
        run: |
          cd private/invent_radha
          cargo +1.98.1 fmt --all -- --check >"$RUNNER_TEMP/radha-fmt.log" 2>&1

      - name: Compile gate
        id: check
        continue-on-error: true
        shell: bash
        run: |
          cd private/invent_radha
          cargo +1.98.1 check --workspace --all-targets >"$RUNNER_TEMP/radha-check.log" 2>&1

      - name: Test gate
        id: tests
        continue-on-error: true
        shell: bash
        run: |
          cd private/invent_radha
          cargo +1.98.1 test --workspace --all-targets >"$RUNNER_TEMP/radha-test.log" 2>&1

      - name: Clippy gate
        id: clippy
        continue-on-error: true
        shell: bash
        run: |
          cd private/invent_radha
          cargo +1.98.1 clippy --workspace --all-targets -- -D warnings >"$RUNNER_TEMP/radha-clippy.log" 2>&1

      - name: Final privacy-safe gate
        shell: bash
        run: |
          echo "RADHA_FMT=${{ steps.fmt.outcome }}"
          echo "RADHA_CHECK=${{ steps.check.outcome }}"
          echo "RADHA_TESTS=${{ steps.tests.outcome }}"
          echo "RADHA_CLIPPY=${{ steps.clippy.outcome }}"

          if [ "${{ steps.fmt.outcome }}" = "success" ] &&
             [ "${{ steps.check.outcome }}" = "success" ] &&
             [ "${{ steps.tests.outcome }}" = "success" ] &&
             [ "${{ steps.clippy.outcome }}" = "success" ]; then
            echo "RADHA_VERIFY_RESULT=PASS"
            exit 0
          fi

          echo "RADHA_VERIFY_RESULT=FAIL"
          exit 1
